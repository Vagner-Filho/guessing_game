// variables1.rs
// Make me compile!
// Execute the command `rustlings hint variables1` if you want a hint :)

fn main() {
    // let x = 5;
    const x: i32 = 5;
    // both of options work. With const, the type of the value must be declared
    println!("x has the value {}", x);
}
